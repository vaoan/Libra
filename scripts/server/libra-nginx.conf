upstream libra_auth    { server 127.0.0.1:5000; }
upstream libra_store   { server 127.0.0.1:5001; }
upstream libra_admin   { server 127.0.0.1:5002; }
upstream libra_landing { server 127.0.0.1:5004; }
upstream libra_pay     { server 127.0.0.1:5005; }
upstream libra_studio  { server 127.0.0.1:5006; }

map $http_upgrade $allowed_upgrade {
    default "";
    "websocket" "websocket";
}
map $http_upgrade $connection_upgrade {
    default "";
    "websocket" "upgrade";
}

server {
    listen 9090;
    server_name _;

    proxy_buffer_size          16k;
    proxy_buffers              8 16k;
    proxy_busy_buffers_size    32k;
    large_client_header_buffers 8 32k;

    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/xml application/json application/javascript application/rss+xml application/atom+xml image/svg+xml;

    location /store      { proxy_pass http://libra_store;   include /home/furrycolombia/libra-proxy.inc; }
    location /admin      { proxy_pass http://libra_admin;   include /home/furrycolombia/libra-proxy.inc; }
    location /auth       { proxy_pass http://libra_auth;    include /home/furrycolombia/libra-proxy.inc; }
    location /payments   { proxy_pass http://libra_pay;     include /home/furrycolombia/libra-proxy.inc; }
    location /studio     { proxy_pass http://libra_studio;  include /home/furrycolombia/libra-proxy.inc; }
    location /           { proxy_pass http://libra_landing; include /home/furrycolombia/libra-proxy.inc; }

    location = /health {
        access_log off;
        return 200 'OK';
        add_header Content-Type text/plain;
    }
}
